"""PostgreSQL audit trail (Milestone 8): models, migrations, repositories.

Depends only on ``core`` (plus SQLAlchemy). Callers map their domain objects to
the record types in :mod:`adaptive_quant.persistence.records`. See ``docs/DATABASE.md``.
"""
