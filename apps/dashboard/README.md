# Adaptive Quant — operator dashboard

Static Next.js + TypeScript dashboard for the operator API. See
[docs/DASHBOARD.md](../../docs/DASHBOARD.md) for pages, security and tests.

```bash
npm ci
npm run dev        # http://localhost:3000; needs `aq --env paper api serve` and AQ_API_TOKEN
```
